#include "common.h"



int main(int argc, char* argv[])
{
    if(init_entropy_calculator(argc, argv) == FAILURE)
    {
        exit(-1);
    }
    // Initialization
        // command loading
        // image loading
        // configuration
    // image processing
        // partitioning
        // transform
        // quantization

    // something statistical    
    return 0;
}